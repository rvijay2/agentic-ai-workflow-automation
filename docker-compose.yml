version: '3.8'
services:
  agent:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - PORT=3000
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
